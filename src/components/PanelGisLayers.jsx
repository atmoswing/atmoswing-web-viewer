import Panel from './Panel';

export default function PanelGisLayers(props) {
    return (
        <Panel title="GIS layers" defaultOpen={props.defaultOpen}>
            <div className="panel-content">
                <p>....</p>
            </div>
        </Panel>
    );
}